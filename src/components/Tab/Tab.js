import React, { useState } from 'react';
// import './Tab.css';
let className = 'tab-list-item';

const Tab = ({ onClick, label }) => {
  if (activeClass)
  return (
    <div></div>
  );
};

export default Tab;